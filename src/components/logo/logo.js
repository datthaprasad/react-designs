import React from "react";
import "./logo.css";

const Logo = (props) => {
  return (
    <div className="logo">
    </div>
  )
  // <img className="img" src={require("./image-container.png")} alt="logo" />;
};

export default Logo;
